# Eureka configuration for CI
eureka:
  client:
    fetchRegistry: false
    registerWithEureka: false

# Management configuration for CI
management:
  opentelemetry:
    tracing:
      export:
        otlp:
          endpoint: http://localhost:4318/v1/traces
  otlp:
    metrics:
      export:
        url: http://localhost:4318/v1/metrics
